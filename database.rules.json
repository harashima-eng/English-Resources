{
  "rules": {
    "exams": {
      "$examId": {
        ".read": true,
        ".write": true
      }
    },
    "answers": {
      "$examId": {
        ".read": true,
        ".write": "auth != null && auth.token.email === 'harashima@komagome.ed.jp'"
      }
    }
  }
}